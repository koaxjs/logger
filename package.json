{
  "name": "@koax/logger",
  "description": "Koax action logger.",
  "repository": "git://github.com/koaxjs/logger.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "redux-logger": "^2.5.2"
  },
  "devDependencies": {
    "@koax/store": "^0.1.1",
    "babel-cli": "^6.4.0",
    "babel-polyfill": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "koax": "^0.4.1",
    "tape": "^4.2.2"
  }
}
